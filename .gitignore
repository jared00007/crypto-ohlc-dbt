# Python / uv
__pycache__/
*.py[cod]
.venv/
.python-version

# dbt
target/
logs/
.user.yml

# dbt_packages/ is deliberately NOT ignored. Vendoring the pinned package makes
# every build hermetic — no hub.getdbt.com call in CI or in sandboxed dev
# environments. packages.yml remains the source of truth for the version.

# DuckDB
*.duckdb
*.duckdb.wal

# Environment
.env

# OS
.DS_Store
